import 'package:flutter/material.dart';

class XemDSDonDatDat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Xem DS Don da dat'),
      ),
    );
  }
}
